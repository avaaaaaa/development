* some basic operations
%, division remainder
*, multiply
/, divide
+, add
-, subtract


* some number methods
Math.round(2.1); // 2
Math.round(2.6); // 3
Math.round(2.5); // 3

Math.floor(2.1); // 2
Math.floor(2.6); // 2
Math.floor(2.5); // 2

Math.ceil(2.1); // 3
Math.ceil(2.6); // 3
Math.ceil(2.5); // 3

Math.random()
// returns a pseudorandom number between 0 and 1


* numeric separator
underscore(_) can be used to separate number characters
let x = 1000000;
let y = 1_000_000;
let z = 100000_0;
x == y; // true
x == z; // true


* string to number, or vice versa
see string.txt

